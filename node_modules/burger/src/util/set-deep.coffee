module.exports = setDeep = (attrs, value, obj = {}) ->
  prop = attrs[0]

  obj[prop] = if attrs.length > 1
                setDeep(attrs.slice(1), value, obj[prop] || {})
              else
                value

  obj

